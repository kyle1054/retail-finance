"""Northwind Deductions Manager application package.

Domain sub-packages: auth, deductions, cards, cash, regional; plus data (data
access) and services (cross-cutting helpers). The Flask app object lives in
`northwind.core`. Kept import-light on purpose so importing `northwind` has no side effects.
"""
